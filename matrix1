rows = 3
cols = 3
matrix = []

for i in range(rows):
    row = []
    for j in range(cols):
        value = int(input(f"Element at ({i},{j}): "))
        row.append(value)
    matrix.append(row)

# Print the matrix
for r in matrix:
    print(r)
